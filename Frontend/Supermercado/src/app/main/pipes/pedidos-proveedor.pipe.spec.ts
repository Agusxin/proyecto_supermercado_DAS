import { PedidosProveedorPipe } from './pedidos-proveedor.pipe';

describe('PedidosProveedorPipe', () => {
  it('create an instance', () => {
    const pipe = new PedidosProveedorPipe();
    expect(pipe).toBeTruthy();
  });
});
